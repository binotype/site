/**
 * Optional polyfill loader for consuming applications.
 * Import this in your main application file if you need the polyfills.
 *
 * @example
 * ```typescript
 * // In your main.ts or app.tsx
 * import "@binotype/site/dist/polyfill-loader"
 * ```
 */
import "./polyfill"
